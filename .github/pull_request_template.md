## Theme

- Theme id:
- New theme or update:
- VRCX-0 version tested:

## Checklist

- [ ] This PR changes one theme only.
- [ ] `theme.json` is strict JSON and matches `schemas/theme.schema.json`.
- [ ] Theme directory name matches `theme.json` `id`.
- [ ] `version` was added or bumped.
- [ ] `author.github` is set.
- [ ] `testedWith` is the latest VRCX-0 version I tested.
- [ ] `license` is set to `GPL-3.0-only` or an approved GPLv3-compatible
      redistributable license.
- [ ] `remoteAssets` accurately reflects remote image or font usage.
- [ ] `tags` use one to three entries.
- [ ] `preview.webp` is included.
- [ ] `preview.webp` is compressed and no larger than 256 KiB.
- [ ] CSS is readable and not minified or obfuscated.
- [ ] CSS does not hide recovery, close, exit, login, confirmation, or
      destructive-action UI.

## Asset Sources

List any remote image, font, or preview sources and their licenses:

-

## Preview

Attach `preview.webp`:
